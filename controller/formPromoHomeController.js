const fs = require('fs')
const path = require('path')
const FormPromo = require('../model/formPromoHome')

module.exports = {
    addFormPromo: async (req, res) => {
        const formPromo = new FormPromo ({
            
        })
    }
}